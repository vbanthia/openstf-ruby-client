require "spec_helper"

describe OpenSTF::Client do
  it "has a version number" do
    expect(OpenSTF::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
